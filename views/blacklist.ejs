<!DOCTYPE html>
<html lang="nl-BE">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blacklisted</title>
    <link type="text/css" rel="stylesheet" href="/css/styles.css" />
 
   <%- include ("partials/header.ejs") %>
  
  <body>
   
    <main>
      <table id="bekijk">
        <thead>
          <tr>
            <th>Afbeelding</th>
            <th>Code</th>
            <th>Set Afbeelding</th>
            <th>Set Code</th>
            <th>Reden</th>
          </tr>
        </thead>
        <tbody>
          
            <% for (let i=0; i < user?.BlackListed?.length  ; i++){ %>
              <tr>
  
                <td>
                  <img height="250" alt="minifig" src=<%=user?.BlackListed?.[i].figUrl %>
                </td>
                <td>
                  <%= user?.BlackListed?.[i].figId %>
                </td>
                <td>
                  <img height="250" alt="minifig" src=<%=user?.BlackListed?.[i].setUrl %>
                </td>
                <td>
                  <%= user?.BlackListed?.[i].setId %>
                </td>

                <td>
                  <%= user?.BlackListed?.[i].reden %>
                </td>

    
                <% }; %>
              </tr>
          
  
        </tbody>
      </table>

      
  <%- include ("partials/footer.ejs") %>