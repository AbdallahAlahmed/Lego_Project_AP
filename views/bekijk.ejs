<!DOCTYPE html>
<html lang="nl-BE">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Bekijken</title>
  <link  rel="stylesheet" href="/css/styles.css" />

<%- include ("partials/header.ejs") %>

<body>
  <main>
    <table id="bekijk">
      <thead>
        <tr>
          <th>Afbeelding</th>
          <th>Code</th>
          <th>Set Afbeelding</th>
          <th>Set Code</th>
        </tr>
      </thead>
      <tbody>
        
          <% for (let i=0; i < user?.ChosenSet?.length  ; i++){ %>

            <tr>

              <td>
               <a href="/user/<%= id %>/<%= user?.ChosenSet?.[i].figId %>/parts"><img height="250" alt="minifig" src=<%=user?.ChosenSet?.[i].figUrl %> </a> 
              </td>
              <td>
                <%= user?.ChosenSet?.[i].figId %>
              </td>
              <td>
               <a href="/user/<%= id %>/<%= user?.ChosenSet?.[i].setId %>/minifigs"><img height="250" alt="minifig" src=<%=user?.ChosenSet?.[i].setUrl %> </a>
              </td>
              <td>
                <%= user?.ChosenSet?.[i].setId %>
              </td>
  
              <% }; %>
            </tr>
        

      </tbody>
    </table>

 <%- include ("partials/footer.ejs") %>